import type { InputStylesType } from "@material-tailwind/react";

const inputTextTheme:InputStylesType  = {
        defaultProps: {
            variant: "standard",
            size: "md",
            color: "white",
        }
    };

export default inputTextTheme;